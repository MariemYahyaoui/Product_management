{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
<div class="container py-5">

    <!-- Page header -->
    <div class="text-center mb-5">
        <h1 class="fw-bold">Edit Category</h1>
        <p class="text-muted">Update your category details.</p>
    </div>

    <!-- Form card -->
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">

            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">

                    {% include 'category/_form.html.twig' with { form: form, button_label: 'Update Category' } %}

                </div>
            </div>

        </div>
    </div>

    <!-- Footer navigation -->
    <div class="text-center mt-4">
        <a href="{{ path('app_index') }}" class="text-decoration-none">
            ← Back to homepage
        </a>
    </div>

</div>
{% endblock %}
